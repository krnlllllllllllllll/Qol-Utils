Editor Build Tools (12 Total)
1. Auto‑Structure Builder
Automatically constructs gameplay structures like slopes, tunnels, wave corridors, and platform sequences.

2. Pattern Repeater
Repeats selected objects with customizable spacing, rotation, scaling, and randomization.

3. Smart Deco Filler
Fills empty areas with decorative elements based on theme presets, glow styles, and color palettes.

4. Auto Group Assigner
Assigns groups to objects automatically using rules based on type, layer, color, or trigger category.

5. Trigger Visualizer
Displays visual overlays for trigger ranges, easing curves, pulse zones, and movement paths.

6. Object Cleanup Tool
Removes unused groups, invisible objects, duplicate colors, and resolves Z‑layer conflicts.

7. Symmetry Builder
Creates mirrored, radial, or multi‑axis symmetry for both gameplay and decoration.

8. Auto Hitbox Preview
Shows hitboxes for blocks, slopes, hazards, and custom objects to ensure gameplay fairness.

9. Multi‑Paste Brush
Allows pasting multiple objects as a brush with random rotation, scale, density, and scatter options.

10. Theme Switcher
Applies full visual themes including color palettes, glow styles, background deco, and pulse presets.

11. AI Deco Assistant
Suggests decorative elements based on gameplay shape, theme, difficulty, and empty space detection.

12. Editor Tools Hub
A unified menu that organizes all tools into a clean, accessible interface inside the editor.

Dependencies
node.ids
Used for advanced object identification, grouping, and editor automation.

Autobuild
Provided by Antiprime  
Marked as unrecommended due to experimental behavior.

Unsupported Feature Limits
QOL Utils restricts certain editor actions when they rely on unsupported or unstable Geode features, ensuring safer builds and fewer crashes.

Unrecommended Mods
These mods may conflict with QOL Utils or cause unstable behavior:

auto-decorator-utils

legacy-autodec

old-style-decorator

unsafe-decorator-pack

Compatibility
Fully compatible with Geode 5.7.1

Designed for modern Geometry Dash editor workflows

Safe fallback behavior when conflicting mods are detected
